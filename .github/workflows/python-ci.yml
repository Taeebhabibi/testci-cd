on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
     
      - name: install python

      
        run: sudo apt update && sudo apt install python3 -y
      - name: run tests
        run: python3 test.py
